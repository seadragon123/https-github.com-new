# 民宿管理系统 — 差距分析 & 最佳方案建议（终版）

> 背景：经营一家**42间客房、含餐厅（对外开放）、请香**业务的民宿酒店  
> 分析对象：`PRD.md v2.0`  
> 已确认：餐厅不分桌管理、支出需上传单据图片

---

## 一、核心数据调整

### 房间数量

```
种子数据：24间 → 42间
楼层/房型按实际配置调整
```

### 餐厅简化：不分桌，只管理客人和菜品

```
之前:                   现在:
┌─ 餐厅 ──────────┐    ┌─ 餐厅 ────────────────┐
│  01桌 │ 02桌    │    │  不区分桌位             │
│  03桌 │ 包间1   │    │  点单时记录：           │
│  包间2│         │    │  ① 住店客人→挂房账      │
└────────────────┘    │  ② 外客→填姓名+电话     │
                       │  ③ 外卖→标记外卖        │
                       └────────────────────────┘

catering_orders 表简化：
  去掉 table_id
  保留: guest_name, guest_phone, booking_id(挂房账时), items, total, payment_method, status
```

### 支出管理：增加单据图片上传

```
expenses 表新增字段：
  receipt_image TEXT DEFAULT ''  ← 存储图片文件路径

上传方案（轻量）：
  ① 前端拍照/选择图片 → base64 编码
  ② 后端保存为文件 → uploads/receipts/20260624_001.jpg
  ③ DB 中存相对路径
  ④ 查看时通过 /uploads/ 静态路由访问

存储目录：
  hotel-manager/
  ├── data/
  ├── uploads/
  │   └── receipts/      ← 支出单据图片
  │       ├── 20260624_001.jpg
  │       └── 20260624_002.jpg
  └── server/
```

---

## 二、更新后的完整差距清单

### 🔴 阶段1 必做

| # | 缺口 | 说明 | 优先级 |
|:-:|------|------|:------:|
| 1 | **退房合并结算** | 餐饮/请香挂账 → 退房时汇总 | 🔴 最高 |
| 2 | **押金收退管理** | 收退有记录，可抵扣消费 | 🔴 |
| 3 | **餐饮点单管理** | 菜品管理+点单+结账（挂房账/直接付） | 🔴 |
| 4 | **请香独立管理** | 商品+销售+库存+返佣+挂房账 | 🔴 |
| 5 | **支出管理+图片上传** | 独立页面+类别统计+拍照上传单据 | 🔴 |
| 6 | **日报自动汇总** | 各业务模块自动拉取 | 🔴 |
| 7 | **数据库备份** | 每小时自动备份 | 🔴 |
| 8 | **42间房种子数据** | 按实际房型配置 | 🔴 |

### 🟡 阶段2 优化

| # | 缺口 | 说明 |
|:-:|------|------|
| 9 | 动态房价日历 | 周末/节假日自动调价 |
| 10 | 交班报表 | 前台交接班清点 |
| 11 | OTA渠道标记 | 携程/美团/飞猪区分 |
| 12 | 发票管理 | 开票记录 |

### 🟢 阶段3 扩展

| # | 缺口 | 说明 |
|:-:|------|------|
| 13 | 换 MySQL | 多台电脑同时用 |
| 14 | 多用户权限 | 角色分工 |
| 15 | 响应式PC端 | 电脑端完整布局 |

---

## 三、修订后的表结构

### expenses（支出）— 增加图片字段

```sql
CREATE TABLE expenses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  report_date TEXT NOT NULL,
  category TEXT NOT NULL DEFAULT '日常耗材',
  amount REAL DEFAULT 0,
  note TEXT DEFAULT '',
  receipt_image TEXT DEFAULT '',  -- 新增：单据图片路径
  created_at TEXT DEFAULT (datetime('now','localtime')),
  updated_at TEXT DEFAULT (datetime('now','localtime'))
);
```

### catering_orders（餐饮订单）— 简化版

```sql
CREATE TABLE catering_orders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  order_no TEXT UNIQUE,             -- 自动生成：CY20260624001
  booking_id INTEGER,               -- 挂房账时关联，NULL=外客
  guest_name TEXT NOT NULL,          -- 客人姓名
  guest_phone TEXT DEFAULT '',       -- 客人电话
  order_type TEXT DEFAULT '堂食',    -- 堂食/外卖
  items TEXT NOT NULL,               -- JSON：菜品明细
  total REAL DEFAULT 0,
  payment_method TEXT DEFAULT '',    -- 现金/微信/支付宝/挂房账
  status TEXT DEFAULT '就餐中',      -- 就餐中/已结账/已取消
  paid_at TEXT,
  created_at TEXT DEFAULT (datetime('now','localtime')),
  updated_at TEXT DEFAULT (datetime('now','localtime'))
);
```

### 新增 uploads 目录结构

```
hotel-manager/
├── server/
│   ├── index.js
│   ├── routes/
│   │   ├── expenses.js     ← 新增：支出独立路由（含图片上传）
│   │   └── ...
│   └── uploads/            ← 新增：上传文件存储
│       └── receipts/       ← 支出单据图片
└── ...
```

---

## 四、图片上传交互设计

### 支出录入弹窗（含拍照）

```
┌─ 新增支出 ───────────────────────┐
│                                  │
│  类别: [日常耗材        ▼]       │
│  金额: [___________] 元          │
│  备注: [________________]        │
│                                  │
│  单据凭证:                        │
│  ┌────────────────────────┐     │
│  │   📷 拍照上传           │     │  ← 点击拍照/选图
│  │   支持 JPG/PNG          │     │
│  └────────────────────────┘     │
│                                  │
│  已上传: 微信图片_20260624.jpg   │
│  [查看大图] [删除]              │
│                                  │
│  [取消]          [确认添加]       │
└──────────────────────────────────┘
```

### 支出列表展示

```
┌──┬────────┬────────┬───────┬──────────┬──────────┐
│# │ 时间   │ 类别   │ 金额  │ 备注     │ 单据     │
├──┼────────┼────────┼───────┼──────────┼──────────┤
│1 │ 9:30   │日常耗材│ ¥150 │ 买纸巾   │ 📎 查看  │  ← 有点击查看大图
│2 │ 10:00  │餐饮成本│ ¥320 │ 食材采购 │ 📎 查看  │
│3 │ 14:30  │维修杂费│ ¥50  │ 灯泡     │ - 无图片 │
└──┴────────┴────────┴───────┴──────────┴──────────┘
```

---

## 五、建议实施路线（终版）

```
阶段1（2-3周）→ 核心跑通
┌──────────────────────────────────────────────────────┐
│ 🔴 42间房种子数据配置                                  │
│ 🔴 数据库自动备份机制                                  │
│ 🔴 退房合并结账（餐饮+请香挂账 → 退房时汇总）           │
│ 🔴 押金收退管理（收/退/扣款记录）                       │
│ 🔴 餐厅点单管理（菜品+点单+结账+挂房账）                │
│ 🔴 请香管理独立页面（商品+销售+返佣+库存）               │
│ 🔴 支出管理独立页面 + 图片上传单据                       │
│ 🔴 日报自动汇总（各业务模块自动拉取）                    │
└──────────────────────────────────────────────────────┘

阶段2（1-2周）→ 精细化
┌──────────────────────────────────────────────────────┐
│ 🟡 动态房价日历（周末/节假日自动调价）                  │
│ 🟡 交班报表（前台/餐厅交接班清点）                      │
│ 🟡 OTA渠道标记（携程/美团/飞猪）                       │
│ 🟡 菜品销量排行                                        │
└──────────────────────────────────────────────────────┘

阶段3（按需）
┌──────────────────────────────────────────────────────┐
│ 🟢 数据库升级（SQLite → MySQL 等多机方案）             │
│ 🟢 多用户权限（前台/餐厅/经理分角色）                   │
│ 🟢 响应式PC端                                         │
└──────────────────────────────────────────────────────┘
```

---

## 六、总结

| 项目 | 调整 |
|------|------|
| 房间数 | 24 → **42间**，种子数据按实际重配 |
| 餐厅管理 | 去掉桌位管理，只管**客人+菜品+结账** |
| 支出管理 | 新增**单据图片上传**，`uploads/receipts/` 目录存储 |
| 数据库备份 | 每小时自动备份，保留30天 |
| 阶段1核心 | 8大任务（2-3周） |

确认后，我从阶段1开始逐一实现。
